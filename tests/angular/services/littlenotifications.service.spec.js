ngDescribe({
    name: 'Test LittlenotificationsService',
    modules: 'app',
    inject: 'LittlenotificationsService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
