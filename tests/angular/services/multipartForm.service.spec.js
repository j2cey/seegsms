ngDescribe({
    name: 'Test MultipartFormService',
    modules: 'app',
    inject: 'MultipartFormService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
