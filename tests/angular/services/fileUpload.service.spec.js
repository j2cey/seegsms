ngDescribe({
    name: 'Test FileUploadService',
    modules: 'app',
    inject: 'FileUploadService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
