ngDescribe({
    name: 'Test campaignplanning-add component',
    modules: 'app',
    element: '<campaignplanning-add></campaignplanning-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
