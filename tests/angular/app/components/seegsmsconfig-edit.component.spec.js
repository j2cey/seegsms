ngDescribe({
    name: 'Test seegsmsconfig-edit component',
    modules: 'app',
    element: '<seegsmsconfig-edit></seegsmsconfig-edit>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
