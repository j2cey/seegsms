ngDescribe({
    name: 'Test campaigns-validate component',
    modules: 'app',
    element: '<campaigns-validate></campaigns-validate>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
