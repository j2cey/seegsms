ngDescribe({
    name: 'Test campaigns-sending component',
    modules: 'app',
    element: '<campaigns-sending></campaigns-sending>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
