ngDescribe({
    name: 'Test campaigns-params component',
    modules: 'app',
    element: '<campaigns-params></campaigns-params>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
